name: Nightly Netlify Build
on:
  schedule:
    - cron: '0 5 * * *'  # Runs at 3 AM UTC daily

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Netlify Build
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/67a0d087c525950e9d80b9cd
